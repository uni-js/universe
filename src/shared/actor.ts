export enum Direction {
    LEFT = "left",
    RIGHT = "right",
    FORWARD = "forward",
    BACK = "back",
}

export enum WalkingState{
    SILENT = "silent",
    WALKING = "walking",
    RUNNING = "running"
}

